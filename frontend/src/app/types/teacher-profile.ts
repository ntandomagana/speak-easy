export interface TeacherProfileData{
    id: string;
    name: string;
    location: string;
    bio: string;
    language: string;
    price: number;
    availability: string;
    profileImageUrl?: string;
}